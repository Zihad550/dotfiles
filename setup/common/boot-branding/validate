#!/usr/bin/env bash

set -euo pipefail

BRANDING_DIR="${1:-$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)}"
# shellcheck disable=SC1091
source "$BRANDING_DIR/lib"
validate_assets "$BRANDING_DIR/plymouth"
echo "Boot Branding assets validated"
