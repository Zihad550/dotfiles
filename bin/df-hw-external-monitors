#!/usr/bin/env bash

drm_path="${DF_HYPR_DRM_PATH:-/sys/class/drm}"

for status in "$drm_path"/card*-*/status; do
    [[ -e $status ]] || continue
    [[ $status =~ -(eDP|LVDS|DSI)-[^/]+/status$ ]] && continue
    [[ $(<"$status") == connected ]] && exit 0
done

exit 1
