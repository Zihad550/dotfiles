#!/usr/bin/env bash

if (($# == 0)); then
   echo "Usage: dotfiles-launch-or-focus-webapp [window-pattern] [url-and-flags...]"
   exit 1
fi

WINDOW_PATTERN="$1"
shift
LAUNCH_COMMAND="dotfiles-launch-webapp $@"

exec dotfiles-launch-or-focus "$WINDOW_PATTERN" "$LAUNCH_COMMAND"
