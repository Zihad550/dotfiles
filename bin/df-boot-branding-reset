#!/usr/bin/env bash

set -euo pipefail

DOTFILES_DIR="${DOTFILES_DIR:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}"
"$DOTFILES_DIR/setup/common/boot-branding/apply"
"$DOTFILES_DIR/setup/common/greeter/apply"
echo "Pinned Boot Branding restored"
