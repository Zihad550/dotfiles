[
  // optional keybindings navigate docs with the same keys as ctrl+w + nav
  {
    "context": "Dock",
    "bindings": {
      "ctrl-w h": "workspace::ActivatePaneLeft",
      "ctrl-w l": "workspace::ActivatePaneRight",
      "ctrl-w k": "workspace::ActivatePaneUp",
      "ctrl-w j": "workspace::ActivatePaneDown",
    },
  },

  // others
  {
    "bindings": {
      "ctrl-b": "workspace::ToggleRightDock",
      "ctrl-shift-b": "workspace::ToggleLeftDock",
      // selection with tab
      "tab": "menu::SelectNext",
      "shift-tab": "menu::SelectPrevious",
      "ctrl-k l": "workspace::ToggleCenteredLayout",
      "alt-b": [
        "task::Spawn",
        {
          "task_name": "File explorer",
          "reveal_target": "center",
        },
      ],
      "ctrl-alt-x": "task::Spawn",
      "ctrl-shift-r": "editor::RestartLanguageServer",
    },
  },
  // others for vim mode
  {
    "context": "vim_mode == normal && !Terminal",
    "bindings": {
      "space e": [
        "task::Spawn",
        {
          "task_name": "File explorer",
          "reveal_target": "center",
        },
      ],
    },
  },

  // switch suggestions
  {
    "context": "Editor && (showing_code_actions || showing_completions)",
    "bindings": {
      "up": "editor::ContextMenuPrevious",
      "shift-tab": "editor::ContextMenuPrevious",
      "down": "editor::ContextMenuNext",
      "tab": "editor::ContextMenuNext",
      "pageup": "editor::ContextMenuFirst",
      "pagedown": "editor::ContextMenuLast",
      // "shift-enter": "menu::Confirm",
      // "enter": "vim::Enter"
    },
  },
  // terminal
  {
    "context": "Terminal",
    "bindings": {
      "ctrl-n": "workspace::NewTerminal",
    },
  },
  // ******
  // **** vim
  // ****
  {
    "context": "Editor && VimControl && !VimWaiting && !menu",
    "bindings": {
      // center when scroll using ctrl-u and ctrl-d
      "ctrl-shift-u": "vim::ScrollUp",
      "ctrl-shift-d": "vim::ScrollDown",
      "ctrl-u": ["workspace::SendKeystrokes", "ctrl-shift-u z z"],
      "ctrl-d": ["workspace::SendKeystrokes", "ctrl-shift-d z z"],
    },
  },
  // center when search
  {
    "context": "Editor && (vim_mode == normal || vim_mode == visual) && !VimWaiting && !menu",
    "bindings": {
      "ctrl-shift-n": "vim::MoveToNextMatch",
      "n": ["workspace::SendKeystrokes", "ctrl-shift-n z z"],
    },
  },

  // use ctrl+p to open file instead of searching for intellisese
  {
    "context": "vim_mode == insert && !(showing_code_actions || showing_completions)",
    "bindings": {
      "ctrl-p": "file_finder::Toggle",
    },
  },
  // inline completionon
  // {
  //   "context": "Editor && inline_completion && !showing_completions",
  //   "bindings": {
  //     "right": "editor::AcceptInlineCompletion"
  //   }
  // }
  {
    "context": "!Terminal",
    "bindings": {
      "ctrl-g ctrl-c": "git::ExpandCommitEditor",
    },
  },
  {
    "context": "GitPanel || GitDiff || GitCommit",
    "bindings": {
      "ctrl-g ctrl-u": "git::Push",
    },
  },
  {
    "context": "Editor",
    "bindings": {
      "ctrl-alt-j": "workspace::OpenInTerminal",
    },
  },
  {
    "context": "Workspace && !Terminal",
    "bindings": {
      "ctrl-p": "file_finder::Toggle",
    },
  },
  {
    "context": "Terminal",
    "unbind": {
      "ctrl-p": "file_finder::Toggle",
    },
  },
  {
    "context": "",
    "bindings": {
      "ctrl-j": "workspace::ToggleBottomDock",
    },
  },
  {
    "context": "Workspace",
    "unbind": {
      "ctrl-alt-b": "workspace::ToggleRightDock"
    }
  },
  {
    "context": "Workspace",
    "bindings": {
      "ctrl-alt-b": "multi_workspace::ToggleWorkspaceSidebar"
    }
  },
  {
    "context": "Workspace",
    "unbind": {
      "ctrl-alt-j": "multi_workspace::ToggleWorkspaceSidebar"
    }
  }
]
